-- MySQL Script generated by MySQL Workbench
-- Tue Oct 22 21:34:20 2024
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema japedidos
-- -----------------------------------------------------
DROP SCHEMA IF EXISTS `japedidos` ;

-- -----------------------------------------------------
-- Schema japedidos
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `japedidos` DEFAULT CHARACTER SET utf8 ;
USE `japedidos` ;

-- -----------------------------------------------------
-- Table `japedidos`.`usuario`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`usuario` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`usuario` (
  `id` INT NOT NULL,
  `nome` VARCHAR(48) NOT NULL,
  `login` VARCHAR(20) NOT NULL,
  `senha` VARCHAR(32) NOT NULL,
  `tipo` ENUM("administrador", "atendente") NOT NULL,
  PRIMARY KEY (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`est_andamento`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`est_andamento` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`est_andamento` (
  `id` INT NOT NULL,
  `nome` VARCHAR(16) NOT NULL,
  PRIMARY KEY (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`cliente`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`cliente` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`cliente` (
  `id` INT NOT NULL,
  `nome` VARCHAR(80) NOT NULL,
  `telefone` VARCHAR(20) NOT NULL,
  `id_usuario_alt` INT NULL COMMENT 'Necessário para controle de alterações, conforme [RNF 06] Controle de alteração de dados',
  `dthr_alt` DATETIME NULL COMMENT 'Necessário para controle de alterações, conforme [RNF 06] Controle de alteração de dados',
  PRIMARY KEY (`id`),
    FOREIGN KEY (`id_usuario_alt`)
    REFERENCES `japedidos`.`usuario` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`pedido`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`pedido` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`pedido` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `id_cliente` INT NOT NULL,
  `id_usuario_autor` INT NOT NULL,
  `dthr_criacao` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `id_usuario_alt` INT NULL COMMENT 'Necessário para controle de alterações, conforme [RNF 06] Controle de alteração de dados\nAo haver alteração em qualquer informação do pedido, deverá haver o registro do autor da alteração e do horário. Alterações no estado do pedido serão registrados independentemente\n',
  `dthr_alt` DATETIME NULL COMMENT 'Necessário para controle de alterações, conforme [RNF 06] Controle de alteração de dados\nAo haver alteração em qualquer informação do pedido, deverá haver o registro do autor da alteração e do horário. Alterações no estado do pedido serão registrados independentemente\n',
  `tipo_entrega` ENUM("envio", "retirada") NOT NULL,
  `dthr_entregar` DATETIME NOT NULL,
  `preco_frete` DECIMAL(8,2) NOT NULL DEFAULT 0.00,
  `tx_desconto` INT NOT NULL DEFAULT 0.00,
  `preco_final` DECIMAL(10,2) NOT NULL,
  `dt_venc_pagamento` DATE NULL,
  `dt_pago` DATE NULL,
  `preco_custo_total` DECIMAL(8,2) NOT NULL,
  PRIMARY KEY (`id`, `id_cliente`),
    FOREIGN KEY (`id_cliente`)
    REFERENCES `japedidos`.`cliente` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`destino`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`destino` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`destino` (
  `id_pedido` INT NOT NULL,
  `logradouro` VARCHAR(45) NOT NULL,
  `numero` VARCHAR(20) NOT NULL,
  `bairro` VARCHAR(45) NOT NULL,
  `cidade` VARCHAR(45) NOT NULL,
  `estado` VARCHAR(45) NOT NULL,
  `pais` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`est_andamento_pedido`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`est_andamento_pedido` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`est_andamento_pedido` (
  `id_est_andamento` INT NOT NULL,
  `id_pedido` INT NOT NULL,
  `id_usuario_autor` INT NOT NULL COMMENT 'Usuário responsável pela atribuição do estado ao pedido',
  `dthr_criacao` DATETIME NOT NULL COMMENT 'Necessário para controle de alterações, conforme [RNF 06] Controle de alteração de dados',
  PRIMARY KEY (`id_est_andamento`, `id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`),
    FOREIGN KEY (`id_est_andamento`)
    REFERENCES `japedidos`.`est_andamento` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`destinatario`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`destinatario` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`destinatario` (
  `id_pedido` INT NOT NULL,
  `info` VARCHAR(120) NOT NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`info_pj`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`info_pj` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`info_pj` (
  `id_pedido` INT NOT NULL,
  `cnpj` VARCHAR(20) NOT NULL,
  `nome_fantasia` VARCHAR(60) NOT NULL,
  `nome_empresarial` VARCHAR(60) NOT NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`info_pf`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`info_pf` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`info_pf` (
  `id_pedido` INT NOT NULL,
  `nome_cliente` VARCHAR(45) NOT NULL,
  `cpf` VARCHAR(11) NOT NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`categoria`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`categoria` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`categoria` (
  `id` INT NOT NULL,
  `nome` VARCHAR(24) NOT NULL,
  `descricao` VARCHAR(120) NULL,
  PRIMARY KEY (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`unidade`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`unidade` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`unidade` (
  `id` INT NOT NULL,
  `nome` VARCHAR(16) NOT NULL,
  `abreviacao` CHAR(5) NOT NULL,
  PRIMARY KEY (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`produto`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`produto` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`produto` (
  `id` INT NOT NULL,
  `id_categoria` INT NOT NULL,
  `id_unidade` INT NOT NULL,
  `nome` VARCHAR(32) NOT NULL,
  `preco_venda` DECIMAL(8,2) NOT NULL DEFAULT 0,
  `preco_custo` DECIMAL(8,2) NOT NULL DEFAULT 0,
  `id_usuario_alt` INT NULL,
  `dthr_alt` DATETIME NULL,
  `estado` BIT(1) NOT NULL DEFAULT b'1',
  PRIMARY KEY (`id`, `id_unidade`, `id_categoria`),
    FOREIGN KEY (`id_categoria`)
    REFERENCES `japedidos`.`categoria` (`id`),
    FOREIGN KEY (`id_unidade`)
    REFERENCES `japedidos`.`unidade` (`id`),
    FOREIGN KEY (`id_usuario_alt`)
    REFERENCES `japedidos`.`usuario` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`produto_pedido`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`produto_pedido` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`produto_pedido` (
  `id_produto` INT NOT NULL,
  `id_pedido` INT NOT NULL,
  `quantidade` INT NOT NULL,
  `preco_venda` DECIMAL(8,2) NOT NULL,
  `preco_custo` DECIMAL(8,2) NOT NULL,
  `info_adicional` VARCHAR(45) NULL,
  PRIMARY KEY (`id_produto`, `id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`),
    FOREIGN KEY (`id_produto`)
    REFERENCES `japedidos`.`produto` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`info_pagamento`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`info_pagamento` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`info_pagamento` (
  `id_pedido` INT NOT NULL,
  `dt_vencimento` DATE NULL,
  `dt_pago` DATE NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`intercorrencia`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`intercorrencia` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`intercorrencia` (
  `id_intercorrencia` INT NOT NULL,
  `id_pedido` INT NOT NULL,
  `descricao` VARCHAR(200) NOT NULL,
  `dthr_criacao` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `id_usuario_autor` INT NOT NULL,
  PRIMARY KEY (`id_intercorrencia`, `id_pedido`, `id_usuario_autor`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`),
    FOREIGN KEY (`id_usuario_autor`)
    REFERENCES `japedidos`.`usuario` (`id`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `japedidos`.`info_cancelamento`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `japedidos`.`info_cancelamento` ;

CREATE TABLE IF NOT EXISTS `japedidos`.`info_cancelamento` (
  `id_pedido` INT NOT NULL,
  `justificativa` VARCHAR(120) NOT NULL,
  PRIMARY KEY (`id_pedido`),
    FOREIGN KEY (`id_pedido`)
    REFERENCES `japedidos`.`pedido` (`id`))
ENGINE = InnoDB;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
